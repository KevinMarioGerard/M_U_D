build:
  docker:
    web: herokuServer/Dockerfile
    image: flaskServer
run:
  web: gunicorn --bind 0.0.0.0:$PORT wsgi
  worker:
    command:
      - gunicorn --bind 0.0.0.0:$PORT wsgi
    image: flaskServer
